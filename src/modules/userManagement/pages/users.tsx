import * as React from "react";
import { BaseComponent } from "@app/common";

export default class Users extends BaseComponent<any>{
    public internalRender(): JSX.Element{
        return (<div>just test content form user module</div>);
    }
}