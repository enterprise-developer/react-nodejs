import * as React from "react";
import {ILayout} from "@app/common";
export class Layout extends React.Component implements ILayout{
    public render():any{
        return (
            <div>Just for testing</div>
        );
    }
}